// frontend/src/contexts/AuthContext.js
import { createContext } from 'react';

// A única responsabilidade deste arquivo é criar e exportar o contexto.
export const AuthContext = createContext(null);
