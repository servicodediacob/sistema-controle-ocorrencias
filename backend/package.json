{
  "name": "sistema-ocorrencias-backend",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "dotenv -e .env.development -- nodemon src/scripts/migrate.js",
    "migrate:dev": "dotenv -e .env.development -- node src/scripts/migrate.js",
    "migrate:prod": "dotenv -e .env.production -- node src/scripts/migrate.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "csv-parse": "^5.5.6",
    "dotenv": "^17.2.2",
    "dotenv-cli": "^7.4.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
