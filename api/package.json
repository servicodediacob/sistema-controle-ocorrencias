{
  "name": "sistema-ocorrencias-api",
  "version": "1.0.0",
  "description": "API for Sistema de Ocorrências",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "migrate": "prisma migrate deploy || prisma db push --accept-data-loss --schema prisma/schema.prisma --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "^5.x.x",
    "express": "^4.x.x",
    "dotenv": "^16.x.x"
  },
  "devDependencies": {
    "@types/express": "^4.x.x",
    "@types/node": "^20.x.x",
    "prisma": "^5.x.x",
    "typescript": "^5.x.x"
  }
}