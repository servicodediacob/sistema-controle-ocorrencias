{
  "name": "sistema-ocorrencias-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd api && npm install && npm run build",
    "start": "cd api && node dist/server.js",
    "migrate": "cd api && npm run migrate",
    "diag": "cd api && npm run diag"
  },
  "workspaces": [
    "api",
    "frontend"
  ]
}
